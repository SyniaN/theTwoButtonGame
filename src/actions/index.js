export const inputLeft = (playerType) => {
    return {
        type: 'LEFT',
        playerType
    }
}

export const inputRight = (playerType) => {
    return {
        type: 'RIGHT',
        playerType
    }
}